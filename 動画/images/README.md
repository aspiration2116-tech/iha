# ここに Lovart で作った絵を入れます

```
cut01.png  cut02.png  …  cut46.png
```

長い場面には `cut10b.png` のように `b` `c` を足せます(その場面の中で自動的に切り替わります)。

- 1920×1080 / 16:9
- 画像に文字を入れない(テロップは後から重ねます)
- プロンプトは `../lovart_プロンプト集.md`

足りない番号は仮画像が入るだけなので、途中まででも `compose_final.py` は動きます。
