# このリポジトリでの作業ルール

YouTubeチャンネル「【明日から役に立つ】ライフハック雑学」（@zubora_lifehack_zatugaku）の
台本制作リポジトリ。台本の型・実測値は `台本/00_型｜このチャンネルの台本テンプレート.md` が正。

## 台本の文字数は「3,300字」で固定（絶対に間違えない）

**「3,300字」とは、Vrew貼り付け用ナレーションtxtの総文字数（改行・句読点を含む）。**

```
python3 台本/尺を数える.py 台本/<台本>.md    # ★Vrew文字数 の行が発注の3,300字
```

- 句読点を除いた実文字数（尺＝毎分354字の計算に使う数字）とは **400〜450字ずれる**。
  実例: ダニ台本3300字版 = Vrew **3,245字** ／ 句読点除き 2,851字。
- 過去にこの2つを取り違え、寝具台本を3,771字まで膨らませて471字の削り直しが発生した。
- **納品前に必ず ★Vrew文字数 で3,300字を確認する。** 指示を毎回聞き直さない。

## 台本を書き換えたら必ず通すコマンド

```
python3 台本/尺を数える.py 台本/<台本>.md --write   # 文字数・各セクションの時刻
python3 台本/断定を洗い出す.py 台本/<台本>.md        # A表/B表の突き合わせ（②〜⑤は必ず0にする）
```

シーン番号（`**Sxx**`）を増減させたら、A表/B表の参照番号も必ず貼り替える。
番号の振り直しは「位置だけを見て一度で連番にする」方式で行う（旧番号と新番号の衝突を避ける）。

## 事実の扱い：A表とB表を必ず分ける

- **A表** = 出典のある事実（URL・発行主体・出典の性質を明記。公的機関か事業者かを区別する）
- **B表** = 台本が独自に決めた運用の目安（出典なし。何から導いたか・何を検証していないかを書く）
- 出典のない話を言い切らない。「〜と言われています」に留める。

## 音声（VOICEVOX / OpenJTalk）の読み間違い対策

Vrew用txtは納品前に pyopenjtalk で読みを実測する。既知の誤読と対処:

| 表記 | 誤読 | 対処 |
|---|---|---|
| 方（かた） | ホウ | **「人」に置き換える**（ユーザー指定） |
| 敷きパッド | ジキパッド | 「しきパッド」に開く |
| 接触冷感 | セッショクヒヤカン | 「ひんやりタイプ」に言い換える |
| 二分・四分 | ニブン・シブン | **半角数字**にする（2分・4分） |
| 天日干し | テンニチボシ | 「てんぴぼし」に開く |
| 後（あと） | ゴ | 「あと」に開く |

字幕と音声は別フィールドなので、**読み用に開いた表記はVrewで音声生成後に字幕だけ漢字へ戻せる。**

## Vrew用ナレーションtxtの形式

`1シーン = 1段落`、段落間は空行1つ。シーン番号・太字記号（`**`）・見出しは除去する。
`台本/2026-09_秋のダニ_3300字版_Vrew用ナレーション.txt` が見本。

## この環境でできないこと（毎回試さない）

組織のネットワークポリシーにより、以下はこのクラウドセッションから到達できない:
lovart.ai / vrew.ai / VOICEVOX（localhost:50021・voicevox.hiroshiba.jp）/ youtube.com / google.com。
ブラウザ自動操作ツールも無い。**Gemini APIドメインのみ到達可能**（画像生成に使える。要APIキー）。
research.db の更新はユーザーがローカルで `research.py` を実行する必要がある。

## .vrew の字幕を1〜2行に直す（毎回手で書き直さない）

```
python3 台本/字幕を改行する.py 入力.vrew 出力.vrew     # 上限29字・検証つき
```

- **字幕(captions)だけを書き換える。** words / assets / tracks / ttsClipInfosMap /
  メディア(mp3・png)には触らないので**音声はそのまま使える**。文字サイズ(150)も不変。
- **上限は29字。30字は実機で行があふれて3行になる恐れがある。**
- 「、」は、削ると語がくっついて読めなくなるものと**列挙のもの**だけ残す。
  列挙の読点を削ると「すきま落ち葉の中積み重ねた木材」のように読めなくなる（一度やった）。
- `integrity` は再計算できない（SHA-256の作り方が不明）。**据え置きでVrewは受け付けた**（2回実績）。
- **Vrew側で「、」の位置にクリップを分割されている場合がある**（137→142件になっていた）。
  その結果**末尾が「、」で終わる字幕**ができるので、行頭・行末の読点は必ず落とす（ツールが対応済み）。
- 文節境界でしか切らないだけでは足りない。**意味のまとまりを割る事故**を実際に出した:
  「家の／外にある」「樹皮の／すきま」「さやが／できた」「応援して／いただける」「物と／物」
  「体が何にも／触れない」「重ねて／置いた」。原因は格助詞「が」と並立助詞「と」を接続助詞と混同したこと、
  「にも」の係助詞を主題の切れ目と誤判定したこと、連体修飾（「終わった株」）を許したこと。

## 音声の作り直しはこの環境では不可

VOICEVOX（localhost:50021・voicevox.hiroshiba.jp）は到達不可、**ffmpeg / lame / sox も無く、
MP3エンコーダが無い**（numpy・wave・audioopのみ）。
したがって**読み間違いが見つかった場合、直せるのは `ttsClipInfosMap[*].text.processed` まで**で、
実際の音声はユーザーがVrewで再生成する必要がある。
**だから納品前のpyopenjtalk検証が重要**（そこで潰しておけば音声を作り直す必要が出ない）。
