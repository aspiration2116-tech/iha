# このリポジトリでの作業ルール

@tomorrow_life_hack（ライフハック雑学チャンネル）の運用リポジトリ。

## Vrew字幕の改行 — 標準ルール

`.vrew` ファイルの字幕を整える依頼が来たら、**必ず `vrew_captions.py` を使う**。
手作業や別ロジックで改行を入れ直さない。

```bash
python3 vrew_captions.py 入力.vrew 出力.vrew レポート.md
```

出力は必ず**別名**にする。元ファイルは絶対に上書きしない。

### 確定している方針（ユーザー承認済み）

- **字幕は1行または2行。3行は作らない。**
- **フォントは150を基本として維持する。** 2行に収まらないクリップだけ、
  収まる最大サイズまで下げる（145/140/135/130/125/120）。
  全クリップを同じサイズに統一する案は不採用。
- 1行の上限は **フォント150で21文字**。動画1920×1080、字幕ボックス幅0.96、
  scaleFactor 0.5625 から算出した実寸。

### 改行の禁則（`vrew_captions.py` に実装済み）

- 行頭に助詞（は・が・を・に・で・と・も・へ・の）を置かない
- 送り仮名を分断しない（「体を冷／やせない」を作らない）
- カタカナ語・熟語・数字の途中で切らない
- 「チャンネル／登録」のようなカタカナ+漢字の複合語を避ける
- ひらがなが連続する箇所で切らない（「そ／して」「冷やさ／れる」を防ぐ）
- 「」の内側では切らない。ただし引用が1行に収まらない長さなら例外的に許可
- 句読点の直後を最優先の改行位置にする
- 「でも」「ただし」「つまり」などの接続詞は行頭に置く

### 触ってよい範囲

書き換えてよいのは `transcript.clips[].captions[].text[]` の
`insert` と `attributes["size"]` **だけ**。

`words` / `tracks` / `assets` / `ttsClipInfosMap` / mp3 / png には触らない。
作業後は必ず次を検証して報告する。

- mp3・pngがバイト単位で元と同一（ハッシュ照合）
- 字幕本文が改行を除いて元と完全一致
- captions以外のJSON構造が元と一致

## Vrewでできないこと（毎回説明が必要）

| 依頼 | 可否 | 理由 |
|---|---|---|
| 字幕の改行 | ○ | `vrew_captions.py` で対応 |
| AI音声の読み修正 | × | mp3は書き出し済み。テキストを直しても音声は変わらず、表示と音声が食い違う。Vrew上で再生成が必要 |
| BGM挿入 | × | project.jsonにBGM用の構造が存在しない（トラックは `ttsClip` と `image` のみ）。推測でJSONを足すとプロジェクトが壊れる |

音声の読みは「要確認箇所のリスト」を出して、ユーザーがVrew上で直す。
特に **「方」の読み分け（ほう／かた）** はこの台本形式で毎回出るので必ず洗い出す。
次いで助数詞（「1つ目」＝ひとつ目、「10個」＝じゅっこ）。

## .vrew ファイルの構造メモ

ZIP（全エントリ無圧縮 `ZIP_STORED`）。書き戻すときは格納順と圧縮方式を保つこと。

```
project.json          プロジェクト全体
media/<mediaId>.mp3   AI音声（1クリップ1ファイル）
media/<mediaId>.png   画像（1クリップ1ファイル）
```

| 場所 | 内容 |
|---|---|
| `transcript.clips[]` | クリップ。`words[]`（読み上げ単位）と `captions[]`（表示字幕） |
| `captions[0].text[].insert` | 字幕本文。改行は `\n` |
| `captions[0].text[].attributes.size` | フォントサイズ（文字列） |
| `props.ttsClipInfosMap[mediaId]` | AI音声のテキスト・話者・速度・ピッチ |
| `props.tracks[]` | `ttsClip`（単語ごとにmp3をスライス）と `image` |
| `props.assets[]` | `trackIds`（複数形）でトラックを参照 |

クリップの分割は `words` の分配とmp3スライスの整合が必要で、
`ttsClipInfosMap` がmediaId単位のため破綻しやすい。**プログラムでは行わない。**
長すぎるクリップはレポートで「手直し推奨」として挙げ、Vrew上で分割してもらう。

## 台本の書き方

既存の台本（`ネタ候補.md` 参照）と同じ体裁に揃える。

- 1文1行。Vrewの「テキストで動画を作成」にそのまま貼れる形にする
- 1行40文字以内
- 10個のポイント形式。5つ目のあとに「ここからが大事です」の中間つなぎを入れる
- 数字が苦手な人向けの前置き → 本編 → まとめ → 行動の後押し → 安全メッセージ →
  アウトロ（ハイプ・高評価・チャンネル登録）
- 4000字で約11分（350字/分換算）
